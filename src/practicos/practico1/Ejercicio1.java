package practicos.practico1;

import TADs.LinkedList.LinkedList;
import TADs.LinkedList.Node;

public class Ejercicio1 {

    public static void main(String[] args) {
        /*No correr porque no funciona gracias*/
        /*LinkedList<Integer> lista = new LinkedList<>();
        lista.add(10);
        lista.add(20);
        lista.add(30);
        lista.add(40);
        lista.addFirst(5);
        lista.addLast(50);

        lista.remove(2);
        Object valorPosicion1 = lista.get(1);

        Node<Integer> n = lista.getHead();
        Node<Integer> prev;

        while (n.getNext() != null) {
            prev = n.getPrev();
            System.out.println("Valor: " + n.getValue());
            if (n.getPrev() != null) {
                System.out.println("Valor previo de " + n.getValue() + ": " + prev.getValue());
            }
            n = n.getNext();
        }
        prev = n.getPrev();
        System.out.println("Valor: " + n.getValue());
        System.out.println("Valor previo de " + n.getValue() + ": " + prev.getValue());

        System.out.println("Existe el 1: " + lista.exists(1));
        System.out.println("Obteniendo valor de la posicion 1: " + valorPosicion1);*/
    }
}
